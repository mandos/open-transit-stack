# gtfs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gtfs` to build the library.

## Running unit tests

Run `nx test gtfs` to execute the unit tests via [Vitest](https://vitest.dev/).
